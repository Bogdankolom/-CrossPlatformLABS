@model Lab2DataModel

@{
	ViewData["Title"] = "№2";
}

<h1>@ViewData["Title"]</h1>
<text> &nbsp; &nbsp; &nbsp;</text>
<h2>Программа которая выведет целое число – сумму элементов найденного прямоугольного подмассива</h2>

<text> &nbsp; &nbsp; &nbsp;</text>

<form method="post" enctype="multipart/form-data" asp-controller="Labs" asp-action="Lab2">
	<div class="form-group">
		<label for="psize">Первая линия</label>
		<input type="text" class="form-control" id="input" asp-for="FirstLine">
	</div>
	<div class="form-group">
		<label for="pdata">Вторая линия</label>
		<input type="text" class="form-control" id="input" asp-for="SecondLine">
	</div>
	<div class="form-group">
		<label for="pdata">Третья линия</label>
		<input type="text" class="form-control" id="input" asp-for="ThirdLine">
	</div>
	<button type="submit" class="btn btn-primary mt-3">Submit</button>
</form>

<text> &nbsp; &nbsp; &nbsp;</text>
<text> &nbsp; &nbsp; &nbsp;</text>

@if (Model != null)
{
	<div class="container mt5">
		<div class="form-group">
			<h2 class="control-label">Результат: @(Model != null ? Model.Calculated : string.Empty)</h2>
		</div>
		@if (Model != null && !string.IsNullOrEmpty(Model.ErrorValue))
		{
			<div class="row">
				<label for="perror"></label>
				<input type="text" id="perror" value="@Model.ErrorValue">
			</div>
		}
	</div>
}